# 2026-04-21 日记

## 重大更新：移动止盈脚本独立化

### 创建独立移动止盈脚本
- 文件: `/root/.openclaw/workspace/trailing_stop.py`
- 启动脚本: `/root/.openclaw/workspace/trailing_stop.sh`
- 功能: 每3秒扫描持仓，盈利>8%激活，从峰值回撤4%止盈
- 独立进程: PID 1931394

### 今日重大Bug修复记录

#### 1. 移动止盈追踪Bug（00:11）
- 症状: GUN回撤超4%但未止盈，用户不满
- 根因1: 峰值更新顺序错误（先更新peak再检查激活）
- 根因2: 主机器人只扫描DB1热点池，持仓跌出DB1后不再被追踪
- 修复1: 调整顺序（先检查退出，再更新peak）
- 修复2: 移动止盈脚本完全独立，从交易所读取持仓

#### 2. 杠杆20x问题（23:47）
- 症状: SPKUSDT/GUNUSDT/BASEDUSDT开出20x杠杆
- 根因: place_order() body里带leverage参数覆盖了set_leverage设置
- 修复: 删除place_order中的leverage字段，只用set_leverage单独设置

#### 3. 止盈参数调整
- 触发线: +8%（不变）
- 退出线: -4%（从-5%改为-4%）
- 回撤计算: 明确用peak计算，做空正确

#### 4. 做空信号加强（23:31）
- 新增ADX>20+DI-确认
- 做空必须ADX>20且DI->DI+
- 量能作为第一优先级判断

## 当前持仓状态（00:26）

| 仓位 | 方向 | 数量 | 杠杆 | 浮盈 | 移动止盈 |
|------|------|------|------|------|---------|
| GUNUSDT | 🔴做空 | 756个 | 10x | +8.6% | ✅已激活 |
| SPKUSDT | 🟢做多 | 737个 | 10x | -1.4% | ⏳未激活 |
| BASEDUSDT | 🔴做空 | 184个 | 10x | -1.4% | ⏳未激活 |

## 今日已平仓

| 币种 | 盈亏 | 类型 |
|------|------|------|
| ORDIUSDT | +$1.96 | 移动止盈 |
| CFGUSDT | +$2.27 | 移动止盈 |
| PIEVERSE | -$9.24 | 止损×4 |
| GUN | -$3.97 | 止损×2 |
| EDGE | -$1.88 | 止损 |
| 合计 | -$10.88 | |

## 进程状态
- 主机器人: PID 1931361
- 移动止盈: PID 1931394

## 文件路径
- 移动止盈脚本: /root/.openclaw/workspace/trailing_stop.py
- 启动脚本: /root/.openclaw/workspace/trailing_stop.sh
- 修复文档: /root/.openclaw/workspace/REPAIR_LOG.md
- 状态汇报: /root/.openclaw/workspace/FUTURES_STATUS_REPORT.md

---

## 重大Bug修复：all-position 返回格式判断错误（01:06）

### 问题描述
trailing_stop.py 获取持仓数据错误，"6个持仓"数据是假的。

### 根因
- Bitget `/api/v2/mix/position/all-position` API 返回 **list**，不是 `dict{'data': list}`
- 原代码：`pos_all = resp['data']` → 对 list 返回 `None` → 误判为0个持仓
- 实际：代码获取到 `None`，但之前"6个持仓"是从缓存/旧日志来的错误数据

### 真实持仓（交易所）
```
SCRTUSDT:   🟢做多 176个  -0.97%  未激活
NAORISUSDT: 🟢做多 326个  +0.46%  未激活
```

### 已被手动平仓（交易所无持仓）
```
GUNUSDT   ❌ 已手动平仓
BASEDUSDT ❌ 已手动平仓
SPKUSDT   ❌ 已手动平仓
```

### 修复内容
1. `db_positions.json` 已清空幽灵记录，只保留真实持仓
2. 解析逻辑：直接用 `resp if isinstance(resp, list) else resp.get('data', [])`
3. 只追踪交易所真实存在的持仓

### 操作记录
- 01:03 - 用户要求暂停主机器人
- 01:06 - 深度检查发现 all-position 返回格式Bug
- 01:06 - 修复完成，db_positions.json 同步

### 当前进程
- 主机器人 (futures_scanner.py): ❌ 已停止（00:43）
- 移动止盈 (trailing_stop.py): ✅ 运行中 (PID 1931394)

### 重要教训
- all-position 返回 list，要直接用，不能 `['data']`
- 不能相信缓存/日志数据，必须验证交易所API原始返回
# 2026-04-21 日记

## 重大更新：移动止盈脚本独立化

### 创建独立移动止盈脚本
- 文件: `/root/.openclaw/workspace/trailing_stop.py`
- 启动脚本: `/root/.openclaw/workspace/trailing_stop.sh`
- 功能: 每3秒扫描持仓，盈利>8%激活，从峰值回撤4%止盈
- 独立进程: PID 1931394

### 今日重大Bug修复记录

#### 1. 移动止盈追踪Bug（00:11）
- 症状: GUN回撤超4%但未止盈，用户不满
- 根因1: 峰值更新顺序错误（先更新peak再检查激活）
- 根因2: 主机器人只扫描DB1热点池，持仓跌出DB1后不再被追踪
- 修复1: 调整顺序（先检查退出，再更新peak）
- 修复2: 移动止盈脚本完全独立，从交易所读取持仓

#### 2. 杠杆20x问题（23:47）
- 症状: SPKUSDT/GUNUSDT/BASEDUSDT开出20x杠杆
- 根因: place_order() body里带leverage参数覆盖了set_leverage设置
- 修复: 删除place_order中的leverage字段，只用set_leverage单独设置

#### 3. 止盈参数调整
- 触发线: +8%（不变）
- 退出线: -4%（从-5%改为-4%）
- 回撤计算: 明确用peak计算，做空正确

#### 4. 做空信号加强（23:31）
- 新增ADX>20+DI-确认
- 做空必须ADX>20且DI->DI+
- 量能作为第一优先级判断

## 当前持仓状态（00:26）

| 仓位 | 方向 | 数量 | 杠杆 | 浮盈 | 移动止盈 |
|------|------|------|------|------|---------|
| GUNUSDT | 🔴做空 | 756个 | 10x | +8.6% | ✅已激活 |
| SPKUSDT | 🟢做多 | 737个 | 10x | -1.4% | ⏳未激活 |
| BASEDUSDT | 🔴做空 | 184个 | 10x | -1.4% | ⏳未激活 |

## 今日已平仓

| 币种 | 盈亏 | 类型 |
|------|------|------|
| ORDIUSDT | +$1.96 | 移动止盈 |
| CFGUSDT | +$2.27 | 移动止盈 |
| PIEVERSE | -$9.24 | 止损×4 |
| GUN | -$3.97 | 止损×2 |
| EDGE | -$1.88 | 止损 |
| 合计 | -$10.88 | |

## 进程状态
- 主机器人: PID 1931361
- 移动止盈: PID 1931394

## 文件路径
- 移动止盈脚本: /root/.openclaw/workspace/trailing_stop.py
- 启动脚本: /root/.openclaw/workspace/trailing_stop.sh
- 修复文档: /root/.openclaw/workspace/REPAIR_LOG.md
- 状态汇报: /root/.openclaw/workspace/FUTURES_STATUS_REPORT.md

---

## 重大Bug修复：all-position 返回格式判断错误（01:06）

### 问题描述
trailing_stop.py 获取持仓数据错误，"6个持仓"数据是假的。

### 根因
- Bitget `/api/v2/mix/position/all-position` API 返回 **list**，不是 `dict{'data': list}`
- 原代码：`pos_all = resp['data']` → 对 list 返回 `None` → 误判为0个持仓
- 实际：代码获取到 `None`，但之前"6个持仓"是从缓存/旧日志来的错误数据

### 真实持仓（交易所）
```
SCRTUSDT:   🟢做多 176个  -0.97%  未激活
NAORISUSDT: 🟢做多 326个  +0.46%  未激活
```

### 已被手动平仓（交易所无持仓）
```
GUNUSDT   ❌ 已手动平仓
BASEDUSDT ❌ 已手动平仓
SPKUSDT   ❌ 已手动平仓
```

### 修复内容
1. `db_positions.json` 已清空幽灵记录，只保留真实持仓
2. 解析逻辑：直接用 `resp if isinstance(resp, list) else resp.get('data', [])`
3. 只追踪交易所真实存在的持仓

### 操作记录
- 01:03 - 用户要求暂停主机器人
- 01:06 - 深度检查发现 all-position 返回格式Bug
- 01:06 - 修复完成，db_positions.json 同步

### 当前进程
- 主机器人 (futures_scanner.py): ❌ 已停止（00:43）
- 移动止盈 (trailing_stop.py): ✅ 运行中 (PID 1931394)

### 重要教训
- all-position 返回 list，要直接用，不能 `['data']`
- 不能相信缓存/日志数据，必须验证交易所API原始返回

---

## 日间重大更新记录（09:00-19:00）

### 1. 杠杆Bug（20x问题）深度修复
- **根因**：`set_leverage` 缺少 `marginMode='isolated'` 参数，Bitget默认全仓crossed导致实际20x
- **修复**：请求体显式加 `marginMode: 'isolated'`，返回值校验，自动修正并推送微信告警
- **受影响**：SCRT/PRLU/TRADOOR 实际开出20x，已修正
- **PID 2062446**，已推送GitHub

### 2. 做空策略重构
- **双模式**：主模式（RSI负背离）+ 暴涨快捷模式（日涨幅≥25%）
- **暴涨快捷模式**：跳过RSI负背离，用高位回落+双重缩量（vr5 AND vr1）
- **ADX上限**：暴涨模式 85→**70**，主模式 75→**60**
- **修复**：ADX>60 全拦截（强趋势不做空），防止逆势被爆
- **独立函数**：`check_short_signal()` + `SHORT_CONFIG` 字典可单独配置

### 3. 微信通知全部禁用
- `alert_to_queue()` 改为 `pass`，开仓/止损/止盈不再推送
- 用户要求：安静运行，不打扰

### 4. 连续4小时上涨条件（后撤销）
- 曾加入主模式前置条件：必须连续4小时上涨才能做空
- 用户认为不科学，已撤销

### 5. GitHub备份
- 仓库：https://github.com/l594594158-dev/bitget-futures-scanner
- 警告：API密钥已泄露在仓库中（用户接受风险，不换Key）

### 6. 持仓恢复Bug修复
- **根因**：重启后只扫DB1热点池，不在DB1的币漏掉
- **修复**：新增 `db_all_pos_symbols.json`，记录所有曾开仓的币种
- **当前持仓**：12个（SCRT/PRLU/BLESS/BSBU/GUNU/NAORISU/BASEDU/UAIU/ARIU/SUPERU/LABU/CETU）

### 7. 双向持仓改造（重大）
- **改动**：信号触发时同时开多+空两个方向
- **止损平仓**：只平触发方向，另一方向保留
- **防重复**：同时检查Bitget持仓+本地记录
- **每币种保证金**：4U（多+空各2U）
- **固定止损**：已删除（只有移动止盈）
- **移动止盈触发**：≥**9.5%**（从6%上调）

### 8. 成交校验保护（新增）
- 开仓后1秒查询Bitget真实保证金
- 实际<预期50%立即推送微信告警

### 当前参数配置
```python
TRAIL_TRIGGER_PCT = 0.095   # 移动止盈激活：盈利≥9.5%
TRAIL_EXIT_PCT = 0.04       # 移动止盈退出：回撤4%
POSITION_SIZE = 2           # 每方向保证金2 USDT
LEVERAGE = 10               # 10x杠杆
COOLDOWN_SECONDS = 300      # 5分钟冷却
# 做空配置
adx_min=20, adx_max=60      # 主模式ADX范围
surge_adx_max=60            # 暴涨模式ADX上限
surge_retrace_min=0.05      # 暴涨模式回落≥5%
require_rsi_div=True        # 主模式必须RSI负背离
surge_mode_enabled=True     # 暴涨快捷模式开启
```

### 当前进程
- 主机器人 (futures_scanner.py): PID 2209793 ✅
- 移动止盈 (trailing_stop.py): PID 1943315 ✅（备用独立进程）

### 今日已实现盈亏（截至18:28）
| 标的 | 盈亏 | 类型 |
|------|------|------|
| UAIUSDT | +9.02 | 多→平 |
| RAVEUSDT | +8.93 | 多→平 |
| BASEDUSDT | +2.12 | 多→平 |
| BASUSDT | +1.03 | 多→平 |
| GUNUSDT | +0.31 | 多→平 |
| IRYSUSDT | -0.99 | 多→平 |
| **合计** | **+20.42 USDT** | |

### 爆仓记录（今日）
- UAIUSDT 多单：连续3次被强平（保证金2U太小+10x杠杆）
- 固定止损已删除，无硬止损保护
- 空单UAIUSDT危险：做空@0.317，现价@0.3676，距强平≈0%

### 修复文档
- `/root/.openclaw/workspace/FUTURES_SCANNER_FIXES.md`（完整记录所有Bug修复）
- 已推送GitHub


---

## 晚间修复（20:49）：移动止盈脚本同步修复 + 主机器人监测循环增强

### 问题描述
用户反映移动止盈任务检测不到自己开的仓。

### 根因分析（20:35-20:49）

**问题1：主机器人只扫DB1热点池**
- `db_all_pos_symbols.json` 记录了13个历史币种
- 但主机器人 `monitor_loop()` 每5秒只扫描DB1热点池
- 不在DB1里的币（如SCRT/PRLU/CETU/SUPER/LABU）完全不会被监测
- 这些币在Bitget上有持仓，但机器人不知道

**问题2：移动止盈脚本不同步db_positions.json**
- `trailing_stop_v2.py` 用自己的 `db_positions_v2.json`
- 与主机器人的 `db_positions.json` 完全独立
- 主机器人开的仓不会自动进入止盈脚本的监控列表

### 修复内容

**修复1：主机器人 `futures_scanner.py` 监测循环增强**
- 启动恢复阶段：同时扫描 DB1 + `db_all_pos_symbols.json` 历史列表
- 每5秒监测循环：同时扫描 DB1 + 历史列表
- 发现新持仓立即补入 `db_positions.json`
- 发现仓位已平（Bitget无持仓但本地有）立即清除

**修复2：`trailing_stop_v2.py` 新增 `sync_from_db2()` 函数**
- 每30秒自动从 `db_positions.json` 同步缺失的仓位到 `db_positions_v2.json`
- 对比双方的 `positions` 字典，缺失的条目从Bitget查询详情后补入
- 启动立即同步一次，之后每6个迭代（5s×6=30s）同步一次
- 新参数：`SYNC_INTERVAL = 30`

### 当前持仓（Bitget真实持仓，20:30）
```
BLESSUSDT: long 3174个
BSBUSDT:   long 1215个
UAIUSDT:   long 54个 + short 54个（双向）
NAORISUSDT: long 279个 + short 279个（双向）
BASEDUSDT: long 176个
ARIAUSDT:  long 157个
LABUSDT:   long 100个
CETUSUSDT: long 90个
SUPERUSDT: long 85个
SCRTUSDT:  long 54个
PRLUUSDT:  long 45个
FIGHTUSDT: long 5102个 + short 5102个（双向）
XANUSDT:   long 2255个 + short 2255个（双向）
RAVEUSDT:  long 11个
```
共12个持仓，其中7个不在DB1（SCRT/PRLU/CETU/SUPER/LABU/ARIA/FIGHT）

### 进程状态（20:49）
- 主机器人 (futures_scanner.py): ❌ 已暂停（待用户修改）
- 移动止盈 (trailing_stop_v2.py): ❌ 已暂停（待用户修改）

### 关键文件
- `db_positions.json` - 主机器人持仓数据库
- `db_positions_v2.json` - 移动止盈脚本数据库
- `db_all_pos_symbols.json` - 历史曾开仓币种列表（13个）
- `db_hot_contracts.json` - DB1热点池（实时，符合条件的币才会被扫描）

### 待用户操作
- 用户正在修改止盈脚本（20:49）
- 计划加入：每30秒自动同步db_positions.json → db_positions_v2.json
